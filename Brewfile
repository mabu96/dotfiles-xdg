# Brewfile — macOS packages for this account.
# Apply:  brew bundle --file=~/dotfiles/Brewfile
# Check:  brew bundle check --file=~/dotfiles/Brewfile
# NOTE: if Homebrew is owned by another account (here /opt/homebrew is owned by `mabue`),
#       run as its owner:  sudo -u mabue -H brew bundle --file=~/dotfiles/Brewfile

cask "session-manager-plugin"   # AWS SSM session plugin; required on PATH by cloudshell-interface (csi)
