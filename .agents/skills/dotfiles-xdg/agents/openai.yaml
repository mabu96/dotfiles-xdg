interface:
  display_name: "Dotfiles Xdg"
  short_description: "Repo-specific patterns and workflows for dotfiles-xdg"
  default_prompt: "Use the dotfiles-xdg repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true